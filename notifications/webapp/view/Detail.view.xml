<mvc:View height="100%" controllerName="com.knpl.dga.notifications.controller.Detail" xmlns:mvc="sap.ui.core.mvc" xmlns:core="sap.ui.core" xmlns:layout="sap.ui.layout" xmlns="sap.m" xmlns:f="sap.f" xmlns:fb="sap.ui.comp.filterbar" xmlns:vm="sap.ui.comp.variants" xmlns:smartFilterBar="sap.ui.comp.smartfilterbar">
    
 <Page busy="{oModelDisplay>/PageBusy}"  busyIndicatorDelay="0" showNavButton="false" showHeader="true" >
        <headerContent>
            <Bar>
                <contentLeft>
                    <Breadcrumbs currentLocationText="{i18n>MainObject} {i18n>Details}" >
                        <Link text="{i18n>MainObject}" press="onNavToHome"/>
                    </Breadcrumbs>
                </contentLeft>
            </Bar>
        </headerContent>
        <content>
            <VBox width="100%" id="oVBoxAddObjectPage">
            </VBox>
        </content>
        <footer>
            <Toolbar>
                <ToolbarSpacer/>
                <Button width="7%" text="{i18n>Cancel}" type="Ghost" press="onNavToHome"/>
                <HBox justifyContent="SpaceBetween" visible="{= ${oModelDisplay>/mode} === 'Edit'}" width="15%">
                  <Button  text="{i18n>Draft}" type="Emphasized" press="onPressSave('DRAFT')"/>
                  <Button visible="{= ${oModelView>/IsLater} === false}"  text="{i18n>Send}" type="Emphasized" press="onPressSave('SCHEDULED')"/>
                  <Button visible="{= ${oModelView>/IsLater} === true}"  text="{i18n>Schedule}" type="Emphasized" press="onPressSave('SCHEDULED')"/>
                </HBox>
                 
            </Toolbar>
        </footer>
    </Page>
</mvc:View>
